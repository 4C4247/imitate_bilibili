<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.bilibili.mapper.CarouselMapper">

    <!-- 查询启用状态的轮播图列表 -->
    <select id="selectEnabledList" resultType="com.example.bilibili.entity.Carousel">
        SELECT * FROM carousel WHERE status = 1 ORDER BY `order` ASC
    </select>

</mapper>
