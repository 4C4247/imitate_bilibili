<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.bilibili.mapper.VideoMapper">

    <!-- 根据分类ID查询视频 -->
    <select id="selectByCategoryId" resultType="com.example.bilibili.entity.Video">
        SELECT * FROM video WHERE category_id = #{categoryId} ORDER BY create_time DESC
    </select>

    <!-- 根据用户ID查询视频 -->
    <select id="selectByUserId" resultType="com.example.bilibili.entity.Video">
        SELECT * FROM video WHERE user_id = #{userId} ORDER BY create_time DESC
    </select>

    <!-- 增加视频观看次数 -->
    <update id="incrementViewCount">
        UPDATE video SET view_count = view_count + 1 WHERE id = #{videoId}
    </update>

    <!-- 增加视频点赞次数 -->
    <update id="incrementLikeCount">
        UPDATE video SET like_count = like_count + 1 WHERE id = #{videoId}
    </update>

    <!-- 增加视频评论次数 -->
    <update id="incrementCommentCount">
        UPDATE video SET comment_count = comment_count + 1 WHERE id = #{videoId}
    </update>

</mapper>
