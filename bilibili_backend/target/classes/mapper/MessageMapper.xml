<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.bilibili.mapper.MessageMapper">

    <!-- 根据接收者ID查询消息列表 -->
    <select id="selectByReceiverId" resultType="com.example.bilibili.entity.Message">
        SELECT * FROM message WHERE receiver_id = #{receiverId} ORDER BY create_time DESC
    </select>

    <!-- 根据发送者ID查询消息列表 -->
    <select id="selectBySenderId" resultType="com.example.bilibili.entity.Message">
        SELECT * FROM message WHERE sender_id = #{senderId} ORDER BY create_time DESC
    </select>

    <!-- 根据接收者ID更新消息状态为已读 -->
    <update id="updateStatusByReceiverId">
        UPDATE message SET status = 1 WHERE receiver_id = #{receiverId}
    </update>

</mapper>
