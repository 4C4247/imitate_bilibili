<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.bilibili.mapper.VideoRecommendMapper">
    <!-- 根据推荐类型获取推荐视频列表 -->
    <select id="getRecommendVideosByType" resultType="com.example.bilibili.entity.VideoRecommend">
        SELECT * FROM video_recommend
        WHERE recommend_type = #{recommendType}
        ORDER BY rank ASC
        LIMIT #{limit}
    </select>

    <!-- 根据推荐类型和排名范围获取推荐视频列表 -->
    <select id="getRecommendVideosByRank" resultType="com.example.bilibili.entity.VideoRecommend">
        SELECT * FROM video_recommend
        WHERE recommend_type = #{recommendType}
        AND rank BETWEEN #{startRank} AND #{endRank}
        ORDER BY rank ASC
    </select>
</mapper>
